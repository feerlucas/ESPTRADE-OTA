# ESPTRADE-OTA

Repositório do firmware OTA do KrakenTrendBotV2 para LILYGO T-Display-S3.

Este projeto usa GitHub Releases para hospedar o `.bin` e um manifest JSON em `ota/manifest/` para a ESP32 consultar atualizações remotamente por HTTPS.
